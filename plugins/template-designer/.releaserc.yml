branches:
  - main
tagFormat: "template-designer-v${version}"
repositoryUrl: "https://github.com/tduniec/template-designer-plugin.git"

plugins:
  - "@semantic-release/commit-analyzer"
  - "@semantic-release/release-notes-generator"
  - "@semantic-release/github"
  - - "@semantic-release/changelog"
    - changelogFile: CHANGELOG.md
  - - "@semantic-release/exec"
    - prepareCmd: yarn set-version ${nextRelease.version}
      publishCmd: yarn npm publish
  - - "@semantic-release/git"
    - assets:
        - package.json
        - CHANGELOG.md
      message: "chore(release): template-designer v${nextRelease.version}\n\n${nextRelease.notes} [skip ci]"
